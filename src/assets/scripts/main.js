'use strict';
import Queue from '@scripts/queue';

const { ImageConverter } = require(`${__dirname}/modules`);

const utils = require(`${__dirname}/modules/utils`);

export default class {
    constructor() {}
}
